# ADR-0014 — Persistencia del graph diferida

**Estado:** Accepted

V1 usa `InMemoryGraphProjectionStore`.

Solo se añade backend persistente después del spike de escala y métricas reales que demuestren necesidad.
