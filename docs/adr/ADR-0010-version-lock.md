# ADR-0010 — Resolución exacta, fingerprint y lock

**Estado:** Accepted

Refs como `stable` se resuelven antes de compilar. El plan guarda versiones/revisions/digests exactos y fingerprint canónico.

`pipeline.lock.yaml` es la representación persistible opcional de esa resolución.
